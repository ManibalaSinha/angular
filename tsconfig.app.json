{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": []
  },
  "architect": {
  "build": {
    "options": {
      "tsConfig": "tsconfig.app.json" // update path if different
    }
  }
}
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
